name: CI
on:
  push:
    branches: [master, main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm install
      - run: npm -w @hna/shared run build
      - run: npx -w @hna/api prisma generate
      - run: npm run typecheck
      - run: npx eslint . --ext .ts,.tsx --max-warnings=0
      - run: npm test
        env:
          JWT_SECRET: ci-secret-long-enough-for-validation
      - run: npm run build
        env:
          JWT_SECRET: ci-secret-long-enough-for-validation

  docker:
    runs-on: ubuntu-latest
    needs: build-and-test
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Log in to Gitea registry
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        uses: docker/login-action@v3
        with:
          registry: git.waterburp.com
          username: Atvriders
          password: ${{ secrets.REGISTRY_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile
          push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/master' }}
          tags: |
            git.waterburp.com/atvriders/ham-net-assistant:latest
            git.waterburp.com/atvriders/ham-net-assistant:${{ github.sha }}
